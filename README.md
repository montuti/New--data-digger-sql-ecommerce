# New--data-digger-sql-ecommerce
SQL project simulating an E-Commerce Store database (MySQL) with Customers, Orders, Products, and OrderDetails tables. Covers CRUD operations, aggregate functions (MAX, MIN, SUM, COUNT), filtering, sorting, and joins.
